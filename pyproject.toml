[project]
name = "genpass"
version = "1.0.0"
description = "Secure password generator CLI"
authors = [{ name = "kilyabin" }]
readme = "README.md"
license = "MIT"
requires-python = ">= 3.8"

[project.scripts]
genpass = "genpass.cli:main"

[tool.setuptools]
packages = ["genpass"]
